# Herdr 2.16.0 实施计划

已确认设计：../specs/2026-09-09-herdr-third-party-tools-design.md。
writing-plans 技能未在当前可用技能中列出，使用本地精简计划继续。

1. 修复 Herdr 默认开关、按目标查询、配置保留与 Bash 路径引用；桥接固定使用当前运行时并关闭 stdout 通知模式。
2. 复用 Panel、来源与配置保存接口实现独立页面，区分检查、确认配置和手动启用；补齐中英文文案。
3. 统一版本 2.16.0，更新受影响的五种 README 和版本日志。
4. 在工作区隔离环境运行回归、Herdr 行为和 UI 检查，构建应用。
5. 验证内置运行时与通知路径；保留安装布局生成 ARM DMG，核对版本、架构、签名和 SHA256。

不修改真实 Herdr 配置、不安装主程序、不推送、不发布。Intel 与真实 Herdr 联调未验证时单独披露。
